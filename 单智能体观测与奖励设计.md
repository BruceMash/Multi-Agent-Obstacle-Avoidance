# 单智能体 DMP-RL 观测与奖励公式整理

## 1. 符号定义

设单智能体在离散时刻 \(t\) 的状态为：

```text
x_t = [p_t, v_t]
```

其中：

```text
p_t ∈ R^3: 当前位置
v_t ∈ R^3: 当前速度
g ∈ R^3: 目标点
O = {o_k}: 静态与动态障碍物集合
B = [b_min, b_max]: workspace 边界
Δt = 0.1: 动力学积分步长
```

当前单智能体配置为：

```text
velocity_clip = [-4, 4]
accelerate_clip = [-4, 4]
goal_tolerance = 0.3
max_steps = 50
```

## 2. 观测公式

单智能体策略观测由局部传感器观测和 DMP 状态观测拼接得到：

```math
o_t = [o_t^{sensor}, o_t^{dmp}]
```

### 2.1 目标相关观测

定义当前位置到目标点的向量和距离：

```math
e_t = g - p_t
```

```math
d_t = \lVert g - p_t \rVert_2
```

目标方向单位向量为：

```math
\hat{e}_t =
\begin{cases}
e_t / d_t, & d_t > \epsilon \\
0, & d_t \le \epsilon
\end{cases}
```

归一化目标距离为：

```math
\bar{d}_t = \mathrm{clip}(d_t / d_{clip}, 0, 1)
```

其中 \(d_{clip}\) 由传感器配置中的 `goal_distance_clip` 给定；若未显式设置，则默认为：

```math
d_{clip} = 2 R_{sense}
```

当前单机配置中：

```text
R_sense = 4.5
d_clip = 9.0
```

### 2.2 LiDAR 扫描观测

单机 LiDAR 使用：

```text
azimuth_bins = 8
elevation_bins = 7
N_rays = 8 × 7 = 56
```

设第 \(m\) 条射线在时刻 \(t\) 测得的最近障碍物距离为 \(r_t^m\)，则归一化扫描值为：

```math
s_t^m = \mathrm{clip}(r_t^m / R_{sense}, 0, 1)
```

当前帧扫描向量为：

```math
s_t = [s_t^1, s_t^2, \cdots, s_t^{56}]
```

上一帧扫描向量为：

```math
s_{t-1} = [s_{t-1}^1, s_{t-1}^2, \cdots, s_{t-1}^{56}]
```

当前单机 LiDAR 感知对象为：

```text
静态障碍物 + 动态障碍物
```

当前单机环境中，workspace 边界未被注册为 LiDAR 障碍物，而是通过边界势场奖励单独约束。

### 2.3 DMP 状态观测

DMP 额外观测为：

```math
o_t^{dmp} = [\phi_t, K_\alpha, K_\beta]
```

其中：

```text
φ_t: DMP phase
K_α: DMP 吸引项系数
K_β: DMP 阻尼相关系数
```

### 2.4 完整观测向量

单智能体完整观测为：

```math
o_t =
[
v_t,
\hat{e}_t,
\bar{d}_t,
s_t,
s_{t-1},
\phi_t,
K_\alpha,
K_\beta
]
```

维度为：

```math
\dim(o_t) = 3 + 3 + 1 + 56 + 56 + 3 = 122
```

## 3. 动作公式

策略输出并不直接作为加速度，而是作为 DMP 调制量：

```math
a_t^{RL} = [f_t, \delta g_t]
```

其中：

```math
f_t ∈ R^3
```

表示 forcing term；

```math
\delta g_t ∈ R^3
```

表示目标偏移量。

当前动作范围为：

```math
f_t \in [-10, 10]^3
```

```math
\delta g_t \in [-1, 1]^3
```

## 4. 近目标动作引导公式

若启用 action guidance，并且智能体距离目标较近，则将策略动作向 zero-action DMP 混合。

定义引导半径：

```math
r_g = 1.0
```

初始引导权重：

```math
w_0 = 0.7
```

衰减步数：

```math
N_{decay} = 500000
```

距离门控为：

```math
\eta_d =
\begin{cases}
1 - d_t / r_g, & d_t < r_g \\
0, & d_t \ge r_g
\end{cases}
```

训练步数门控为：

```math
\eta_t = \max(0, 1 - n_t / N_{decay})
```

最终引导权重为：

```math
\lambda_t = \mathrm{clip}(w_0 \eta_d \eta_t, 0, 1)
```

引导后的动作：

```math
\tilde{a}_t^{RL} = (1 - \lambda_t) a_t^{RL}
```

当未启用引导或 \(d_t \ge r_g\) 时：

```math
\tilde{a}_t^{RL} = a_t^{RL}
```

## 5. DMP 控制公式

策略动作经过解析后得到：

```math
\tilde{a}_t^{RL} = [\tilde{f}_t, \widetilde{\delta g}_t]
```

有效目标点为：

```math
g_t^{eff} = g + \widetilde{\delta g}_t
```

forcing 门控为：

```math
q_t = \tanh(|g_t^{eff} - p_t|)
```

DMP 计算得到的期望加速度为：

```math
a_t^{cmd}
=
\frac{
K_\alpha
\left[
K_\beta (g_t^{eff} - p_t)
- \tau v_t
\right]
+ \tilde{f}_t \odot q_t
}{\tau^2}
```

当前 DMP 参数为：

```text
K_alpha = 3.0
K_beta = 0.8
tau = 2.5
```

实际施加到动力学模型的加速度为：

```math
a_t =
\mathrm{clip}(a_t^{cmd}, a_{min}, a_{max})
```

其中：

```math
a_{min} = -4,\quad a_{max} = 4
```

## 6. 动力学更新公式

单智能体采用质点动力学模型。速度更新为：

```math
v_{t+1}
=
\mathrm{clip}(v_t + a_t \Delta t, v_{min}, v_{max})
```

位置更新为：

```math
p_{t+1}
=
p_t + v_t \Delta t + \frac{1}{2} a_t \Delta t^2
```

当前速度裁剪范围为：

```math
v_{min} = -4,\quad v_{max} = 4
```

## 7. Reward 公式

单步 reward 由目标进度奖励、障碍物势场惩罚、边界势场惩罚、固定步长惩罚以及终止奖励组成。

总体形式为：

```math
r_t =
r_t^{prog}
- P_t^{obs}
- P_t^{bd}
- c_{step}
- c_{col}\mathbb{I}_{col}
+ R_{succ}\mathbb{I}_{succ \land \neg col}
- c_{timeout}\mathbb{I}_{timeout}
```

其中：

```text
c_step = 0.01
c_col = 20.0
R_succ = 300.0
c_timeout = 20.0
```

### 7.1 目标进度奖励

执行动作前的目标距离为：

```math
d_t = \lVert g - p_t \rVert_2
```

执行动作后的目标距离为：

```math
d_{t+1} = \lVert g - p_{t+1} \rVert_2
```

距离进度为：

```math
\Delta d_t = d_t - d_{t+1}
```

目标进度奖励为：

```math
r_t^{prog} = w_p \Delta d_t
```

当前：

```math
w_p = 8.0
```

若智能体靠近目标，则 \(\Delta d_t > 0\)，该项为正；若远离目标，则该项为负。

### 7.2 策略动作方向

障碍物势场和边界势场均使用策略动作方向与速度方向进行门控。策略动作诱导的控制方向定义为：

```math
m_t =
K_\alpha K_\beta \widetilde{\delta g}_t
+ \tilde{f}_t \odot \tanh(|g_t^{eff} - p_t|)
```

若 \(\lVert m_t \rVert_2 > \epsilon\)，则：

```math
\hat{m}_t = \frac{m_t}{\lVert m_t \rVert_2}
```

否则不计算势场方向门控，势场惩罚为 0。

速度方向定义为：

```math
\hat{v}_t =
\begin{cases}
v_t / \lVert v_t \rVert_2, & \lVert v_t \rVert_2 > \epsilon \\
0, & otherwise
\end{cases}
```

### 7.3 统一势场惩罚

障碍物与边界采用相同的势场惩罚形式。设 \(S\) 表示某一类风险源集合，\(s \in S\) 表示其中一个风险源，则该类势场惩罚统一写为：

```math
P_t^{S}
=
\min
\left(
w_S
\sum_{s \in S_t}
\left(
\frac{1}{\max(\rho_s, \epsilon_S)}
- \frac{1}{d_0^S}
\right)^2
\gamma_s,
P_S^{max}
\right)
```

其中：

```math
S_t = \{s \in S \mid \rho_s < d_0^S\}
```

```math
\gamma_s =
\max(0, \hat{m}_t^\top \hat{u}_s)
\left[
0.5 + 0.5\max(0, \hat{v}_t^\top \hat{u}_s)
\right]
```

这里，\(\rho_s\) 为当前位置到风险源表面的距离，\(\hat{u}_s\) 为从智能体指向风险源的单位方向，\(d_0^S\) 为势场影响半径，\(\epsilon_S\) 为距离下界截断项，\(w_S\) 为势场权重，\(P_S^{max}\) 为单类势场惩罚上限。

### 7.4 势场参数对应

障碍物势场与边界势场只在风险源定义和参数取值上不同：

| 项目 | 障碍物势场 \(P_t^{obs}\) | 边界势场 \(P_t^{bd}\) |
|---|---|---|
| 风险源集合 \(S\) | 静态障碍物 + 动态障碍物 | workspace 六个边界面 |
| 距离 \(\rho_s\) | 到障碍物表面最近点的距离 | 到对应边界面的有符号距离 |
| 方向 \(\hat{u}_s\) | 指向障碍物表面最近点 | 指向对应边界外侧 |
| 影响半径 \(d_0^S\) | 1.5 | 0.6 |
| 距离下界 \(\epsilon_S\) | \(10^{-3}\) | \(10^{-3}\) |
| 权重 \(w_S\) | 1.5 | 0.3 |
| 上限 \(P_S^{max}\) | 20.0 | 20.0 |

需要注意，当前单智能体环境中，障碍物同时参与 LiDAR 感知和硬碰撞判定；边界只作为势场惩罚项存在，不作为 LiDAR 障碍物，也不作为硬碰撞终止条件。

### 7.5 成功奖励

成功条件为：

```math
\mathbb{I}_{succ}
=
\mathbb{I}
\left[
d_{t+1} \le \epsilon_g
\right]
```

其中：

```math
\epsilon_g = 0.3
```

若成功且未发生碰撞，则：

```math
r_t \leftarrow r_t + R_{succ}
```

当前：

```math
R_{succ} = 300.0
```

### 7.6 碰撞惩罚

碰撞由障碍物几何模型判定：

```math
\mathbb{I}_{col}
=
\mathbb{I}
\left[
\exists o_k \in O,\; p_{t+1} \in o_k
\right]
```

碰撞时：

```math
r_t \leftarrow r_t - c_{col}
```

当前：

```math
c_col = 20.0
```

### 7.7 超时惩罚

若 episode 达到最大步数且未成功、未碰撞，则触发超时：

```math
\mathbb{I}_{timeout}
=
\mathbb{I}
\left[
t \ge T_{max}
\land \neg succ
\land \neg col
\right]
```

当前：

```math
T_max = 50
```

超时惩罚为固定值：

```math
r_t \leftarrow r_t - c_{timeout}
```

当前：

```math
c_timeout = 20.0
```

## 8. 终止条件

单智能体 episode 的终止条件为：

```math
terminated =
\mathbb{I}_{succ}
\lor
\mathbb{I}_{col}
```

超时截断条件为：

```math
truncated =
\mathbb{I}_{timeout}
```

其中：

```text
success: 到达目标半径
collision: 与静态或动态障碍物发生几何碰撞
timeout: 达到最大步数但未成功、未碰撞
```

## 9. 单智能体与多智能体公式差异

单智能体版本没有智能体间观测与智能体间 reward，因此不存在：

```math
o_t^{inter-agent}
```

也不存在：

```math
P_t^{inter-agent}
```

其 reward 只包含：

```math
r_t =
r_t^{prog}
- P_t^{obs}
- P_t^{bd}
- c_{step}
- c_{col}\mathbb{I}_{col}
+ R_{succ}\mathbb{I}_{succ \land \neg col}
- c_{timeout}\mathbb{I}_{timeout}
```

而多智能体版本额外包含：

```math
P_t^{inter-agent}
```

并且会将其他智能体作为动态障碍物和显式相对观测共同输入策略。
