# 多智能体 DMP-MAPPO 观测与奖励设计

## 1. 设计目标

当前环境面向多智能体三维避障与目标到达任务，采用共享策略的 MAPPO 训练结构。每个智能体基于局部观测输出 DMP 调制动作，环境再通过二阶 DMP 控制器将策略动作转换为实际加速度。

观测设计主要用于支持三类决策：

- 面向目标的收敛控制；
- 面向障碍物和边界的局部避障；
- 面向其他智能体的相对避让。

奖励设计主要用于形成四类训练信号：

- 目标距离缩短带来的进度奖励；
- 近目标区域的分段引导奖励；
- 障碍物、边界和智能体间安全约束；
- 成功、碰撞和超时终止条件下的稀疏反馈。

## 2. 观测设计

当前环境采用每个智能体独立观测、共享策略训练的形式。整体观测张量为：

```text
(num_agents, obs_dim) = (5, 456)
```

单个智能体的观测由三部分组成：

```text
obs_i = [sensor_obs_i, dmp_extra_i, inter_agent_obs_i]
```

### 2.1 局部传感器观测

`sensor_obs_i` 由速度、目标信息和局部 3D LiDAR 扫描组成：

| 模块 | 维度 | 说明 |
| --- | ---: | --- |
| 当前速度 `v_i` | 3 | 智能体当前三维速度 |
| 目标方向单位向量 | 3 | 从当前位置指向目标点的单位向量 |
| 目标距离归一化标量 | 1 | 基于感知半径裁剪后的归一化距离 |
| 当前帧 LiDAR 扫描 | 216 | `24 × 9` 多束三维激光雷达 |
| 上一帧 LiDAR 扫描 | 216 | 用于反映障碍物时序变化 |

因此：

```text
sensor_obs_dim = 3 + 3 + 1 + 216 + 216 = 439
```

LiDAR 的感知对象包括：

- 静态障碍物；
- 动态障碍物；
- workspace 六个边界面；
- 其他智能体封装得到的动态障碍物。

其中，其他智能体不仅通过显式友方观测进入策略，也会被封装为动态障碍物进入 LiDAR 扫描，从而形成局部安全感知。

### 2.2 DMP 内部状态观测

`dmp_extra_i` 用于向策略提供 DMP 当前控制状态：

| 变量 | 维度 | 说明 |
| --- | ---: | --- |
| `phase` | 1 | DMP 相位变量 |
| `K_alpha` | 1 | DMP 吸引项系数 |
| `K_beta` | 1 | DMP 阻尼相关系数 |

因此：

```text
dmp_extra_dim = 3
```

### 2.3 多智能体相对观测

`inter_agent_obs_i` 当前只保留最近 `2` 个其他智能体：

```text
nearest_agent_observation_count = 2
```

每个邻居包含：

| 模块 | 维度 | 说明 |
| --- | ---: | --- |
| 相对位置 | 3 | `(p_j - p_i) / inter_agent_influence_distance` |
| 相对速度 | 3 | `(v_j - v_i) / velocity_scale` |
| 归一化距离 | 1 | `||p_j - p_i|| / inter_agent_influence_distance` |

因此：

```text
inter_agent_obs_dim = 2 × 7 = 14
```

### 2.4 单智能体观测维度

单个智能体最终观测维度为：

```text
obs_dim = sensor_obs_dim + dmp_extra_dim + inter_agent_obs_dim
        = 439 + 3 + 14
        = 456
```

## 3. 动作与 DMP 控制接口

策略动作不直接作为加速度执行，而是作为 DMP 调制量输入控制器。单个智能体动作由两部分组成：

```text
action_i = [forcing_term_i, goal_offset_i]
```

其中：

| 动作项 | 维度 | 说明 |
| --- | ---: | --- |
| `forcing_term_i` | 3 | 对 DMP 加速度项的非线性调制 |
| `goal_offset_i` | 3 | 对局部有效目标点的偏移调制 |

当前动作范围为：

```text
forcing_term ∈ [-10, 10]
goal_offset ∈ [-1, 1]
```

DMP 参数为：

```text
K_alpha = 3.2
K_beta  = 0.8
tau     = 2.5
dt      = 0.1
```

在零动作条件下，该 DMP 对应连续时间近似下的临界阻尼系统，用于提供稳定的目标吸引趋势。

## 4. Reward 设计

当前 reward 按智能体分别计算，主要由目标进度、近目标奖励、安全惩罚、碰撞惩罚和成功奖励构成。

单步基础形式为：

```text
r_i =
    step_reward_i
  + near_goal_bonus_i
  - obstacle_potential_penalty_i
  - boundary_potential_penalty_i
  - inter_agent_potential_penalty_i
  - step_penalty
```

其中：

```text
step_penalty = 0.01
```

### 4.1 目标进度奖励

目标进度奖励基于相邻两步的目标距离变化：

```text
step_reward_i = step_reward_weight × (d_prev_i - d_cur_i)
```

当前参数为：

```text
step_reward_weight = 4.0
```

该项用于鼓励智能体持续缩短到目标点的距离。如果智能体远离目标，则该项变为负值。

### 4.2 近目标分段奖励

近目标奖励用于缓解终点附近奖励稀疏的问题。当前设计为：

```text
distance < 1.0 m: +0.2
distance < 0.6 m: +0.6
```

该奖励仅作用于尚未成功的智能体，并且要求：

```text
distance > goal_tolerance
```

当前目标判定半径为：

```text
goal_tolerance = 0.3
```

### 4.3 障碍物势场惩罚

障碍物势场惩罚作用于静态障碍物和动态障碍物，不包含 workspace 边界。当前形式采用方向门控机制：

```text
只有策略动作方向朝向障碍物时，才施加势场惩罚。
```

势场计算采用平方反比形式：

```text
penalty ∝ (1 / d - 1 / influence_distance)^2
```

当前参数为：

```text
obstacle_potential_weight = 2.0
obstacle_influence_distance = 1.5
obstacle_potential_penalty_max = 20.0
```

### 4.4 边界势场惩罚

workspace 边界被封装为 LiDAR 可观测的六个边界面，但边界惩罚单独计算，不混入障碍物势场惩罚。

边界势场同样采用方向门控机制：

```text
只有策略动作方向或速度趋势朝向边界时，才施加边界势场惩罚。
```

当前参数为：

```text
boundary_influence_distance = 0.6
boundary_potential_weight = 0.3
boundary_potential_penalty_max = 20.0
boundary_distance_epsilon = 1e-3
```

### 4.5 智能体间势场惩罚

智能体间势场惩罚基于 pairwise distance 计算：

```text
distance < inter_agent_influence_distance 时触发
```

当前参数为：

```text
inter_agent_safe_distance = 0.6
inter_agent_influence_distance = 1.2
inter_agent_potential_weight = 1.0
```

势场形式为：

```text
penalty_ij = inter_agent_potential_weight × (1 / d_ij - 1 / influence_distance)^2
```

该惩罚同时分配给相互靠近的两个智能体，用于形成近距离排斥约束。

### 4.6 碰撞惩罚与终止

当前碰撞类型分为三类：

- 障碍物碰撞；
- 智能体间碰撞；
- 边界碰撞。

任意碰撞都会终止 episode。碰撞惩罚为：

```text
obstacle_collision:    -80
boundary_collision:    -80
inter_agent_collision: -20
```

当前参数为：

```text
collision_penalty = 80.0
inter_agent_collision_penalty = 20.0
```

### 4.7 成功奖励

当某个智能体首次进入目标半径时，若当前 step 未发生碰撞，则获得成功奖励：

```text
success_reward_i = success_bonus / num_agents
```

当前参数为：

```text
success_bonus = 600.0
num_agents = 5
success_reward_i = 120.0
```

成功后，该智能体会被冻结位置和速度，避免到达目标后继续被随机动作驱动离开目标区域。

### 4.8 超时惩罚

当 episode 达到最大步数且未成功时，对未完成智能体施加剩余距离惩罚：

```text
timeout_penalty_i =
    timeout_penalty × final_distance_i / workspace_diagonal
```

当前参数为：

```text
timeout_penalty = 20
max_steps = 200
```

该设计用于区分“接近目标但未完成”和“距离目标仍很远”的 timeout 状态。

### 4.9 控制惩罚状态

当前代码中已经计算了两类控制惩罚：

```text
acceleration_penalty
acceleration_clip_penalty
```

但它们在 reward 总和中被注释掉，因此当前不参与训练，只作为诊断信息写入 `info`。

## 5. Action Guidance 机制

当前还引入了近目标动作引导机制。该机制不是 reward，而是在动作层面对策略输出进行早期训练约束。

机制形式为：

```text
当 distance < action_guidance_radius 时，
将策略动作向 zero-action DMP 混合。
```

当前参数为：

```text
action_guidance_enabled = True
action_guidance_radius = 1.0
action_guidance_initial_weight = 0.7
action_guidance_decay_steps = 500000
```

引导权重随训练步数线性衰减：

```text
training_gate = 1 - action_guidance_step / action_guidance_decay_steps
```

该机制的作用是让 DMP 在终点附近接管末端收敛，从而减少策略随机 forcing 和 goal_offset 对终点稳定性的破坏。

## 6. 当前设计特点与问题

当前观测设计已经包含其他智能体的位置和速度信息，也通过 LiDAR 将其他智能体作为动态障碍物进行局部感知。因此，策略具备学习基本避让行为的输入条件。

但是，当前多智能体避让信号仍然存在两个限制：

1. 显式友方观测只包含最近 `2` 个智能体，复杂交叉场景下可能不足；
2. inter-agent reward 主要基于距离势场，尚未显式引入 closing speed、time-to-collision 或 closest-approach distance。

因此，当前策略能够感知“附近是否存在其他智能体”，但对“未来是否会形成相向接近和交叉碰撞”的预测信号仍然偏弱。这也是 `lane_swap` 场景比普通并行到达场景更难的主要原因之一。


